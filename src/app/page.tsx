// Minimal placeholder page. The real experience (3D lab, prediction UI,
// mastery display) is owned by Contributor A — this file exists only so the
// app builds and the API routes are reachable.
export default function Home() {
  return (
    <main>
      <h1>Counterfactual Lab</h1>
      <p>
        API is live at <code>/api/compile</code> and <code>/api/evaluate</code>.
        UI under construction.
      </p>
    </main>
  );
}
